{
  "name": "picknotch",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "@astrojs/cloudflare": "11",
    "astro": "^4.16.18"
  },
  "devDependencies": {
    "puppeteer": "^25.1.0",
    "wrangler": "^4.103.0"
  }
}
